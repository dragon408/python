a = set('qwerty')
b = frozenset('qwerty')
print(a == b)

#The only difference between set and frozenset is that set is a mutable datatype, but frozenset is not.

#Единственное отличие set от frozenset заключается в том, что set - изменяемый тип данных, а frozenset - нет