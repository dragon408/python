import numpy as np #library import

a = np.array([[1, 2, 3],
             [12, 23, 34]])#creating an array
print(a) #array output

#usually this library is used for matrices,
#but with such arrays you can use the same
#bases and sorts as with ordinary ones
